---
title: Confirm 确认框
description: 当需要执行一个确定的操作时，使用 Confirm 确认框询问用户。
radix:
  title: Alert Dialog
  link: https://www.radix-ui.com/docs/primitives/components/alert-dialog
---

import Code from "@demo/code";
import Basic from "@demo/confirm/basic";
import MaskClosable from "@demo/confirm/maskClosable";
import TitleIcon from "@demo/confirm/titleIcon";
import IconText from "@demo/confirm/iconText";

## 代码示例

### 引入

```js copy
import { Confirm } from "@ltopx/lx-ui";
```

### 基础用法

<Code 
  description='提供与 Button 按钮 相同的五种基础类型确认框。点击“确认”按钮时触发 onOk 回调' 
  component={<Basic />}
>
```tsx filename="Confirm.tsx" copy
import { Confirm, Button } from "@ltopx/lx-ui";

export default function () {
  const content =
    "Lorem, ipsum dolor sit amet consectetur adipisicing elit. Eum quo ab quis odio maxime, distinctio ea nesciunt provident cumque tempora libero officia fuga accusamus consequuntur ipsum recusandae facere voluptatem sapiente.";
    
  const onOk = () => {
    console.log("ok");
  };
  
  return (
    <div className="flex flex-wrap gap-x-3 gap-y-2">
      <Confirm
        title="这是一个标题"
        content={content}
        type="primary"
        onOk={onOk}
      >
        <Button type="primary">Confirm</Button>
      </Confirm>
      <Confirm
        title="这是一个标题"
        content={content}
        disabled
        type="primary"
        onOk={onOk}
      >
        <Button type="primary" disabled>
          Confirm
        </Button>
      </Confirm>
      <Confirm
        title="这是一个标题"
        content={content}
        type="success"
        onOk={onOk}
      >
        <Button type="success">Confirm</Button>
      </Confirm>
      <Confirm
        title="这是一个标题"
        content={content}
        type="warning"
        onOk={onOk}
      >
        <Button type="warning">Confirm</Button>
      </Confirm>
      <Confirm title="这是一个标题" content={content} type="danger" onOk={onOk}>
        <Button type="danger">Confirm</Button>
      </Confirm>
    </div>
  );
}
```
</Code>

### 点击遮罩层关闭

<Code 
  description='当 maskClosable 设置为true时，点击遮罩层也可以关闭确认框' 
  component={<MaskClosable />}
>
```tsx filename="Confirm.tsx" copy {16}
import { Confirm, Button } from "@ltopx/lx-ui";

export default function () {
  const content =
    "Lorem, ipsum dolor sit amet consectetur adipisicing elit. Eum quo ab quis odio maxime, distinctio ea nesciunt provident cumque tempora libero officia fuga accusamus consequuntur ipsum recusandae facere voluptatem sapiente.";
    
  const onOk = () => {
    console.log("ok");
  };
  
  return (
    <Confirm
      title="这是一个标题"
      content={content}
      type="primary"
      maskClosable
      onOk={onOk}
    >
      <Button type="primary">Confirm</Button>
    </Confirm>
  );
}
```
</Code>

### 自定义标题按钮

<Code 
  description='Confirm 确认框标题默认显示按钮，也可以自定义显示其他按钮' 
  component={<TitleIcon />}
>
```tsx filename="Confirm.tsx" copy {14}
import { Confirm, Button } from "@ltopx/lx-ui";

export default function () {
  const content =
    "Lorem, ipsum dolor sit amet consectetur adipisicing elit. Eum quo ab quis odio maxime, distinctio ea nesciunt provident cumque tempora libero officia fuga accusamus consequuntur ipsum recusandae facere voluptatem sapiente.";
    
  const onOk = () => {
    console.log("ok");
  };
  
  return (
    <Confirm
      title="自定义标题按钮"
      icon={<Loading_line className="animate-spin" />}
      content={content}
      type="primary"
      onOk={onOk}
    >
      <Button type="primary">Confirm</Button>
    </Confirm>
  );
}
```
</Code>

### 自定义底部按钮文字

<Code 
  description='可以自定义底部按钮的文字' 
  component={<IconText />}
>
```tsx filename="Confirm.tsx" copy {17,18}
import { Confirm, Button } from "@ltopx/lx-ui";

export default function () {
  const content =
    "Lorem, ipsum dolor sit amet consectetur adipisicing elit. Eum quo ab quis odio maxime, distinctio ea nesciunt provident cumque tempora libero officia fuga accusamus consequuntur ipsum recusandae facere voluptatem sapiente.";
    
  const onOk = () => {
    console.log("ok");
  };
  
  return (
    <Confirm
      title="这是一个标题"
      content={content}
      type="success"
      onOk={onOk}
      cancelText="取 消"
      okText="确 定"
    >
      <Button type="success">Confirm</Button>
    </Confirm>
  );
}
```
</Code>

## API

### Confirm Props

| 属性             | 描述                 | 类型                                                         | 默认值    |
| ---------------- | -------------------- | ------------------------------------------------------------ | --------- |
| className        | 内容区域类名         | `string`                                                     | -         |
| overlayClassName | 遮罩层类名           | `string`                                                     | -         |
| maskClosable     | 是否可点击遮罩层关闭 | `boolean`                                                    | -         |
| disabled         | 是否禁用             | `boolean`                                                    | -         |
| type             | 确认框类型           | `default` \| `primary` \| `success` \| `warning` \| `danger` | `primary` |
| icon             | 自定义标题按钮       | `React.ReactNode`                                            | -         |
| title            | 自定义标题           | `React.ReactNode`                                            | Title     |
| content          | 自定义内容           | `React.ReactNode`                                            | Content   |
| okText           | 自定义确认按钮文字   | `React.ReactNode`                                            | Ok        |
| cancelText       | 自定义取消按钮文字   | `React.ReactNode`                                            | Cancel    |
| onOk             | 点击“确认”按钮回调   | `(e: Event) => void`                                         | -         |
